apiVersion: apps/v1
kind: Deployment
metadata:
  name: slack-metrics-deployment
spec:
  template:
    spec:
      containers:
        - name: slack-metrics
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 200m
              memory: 200Mi
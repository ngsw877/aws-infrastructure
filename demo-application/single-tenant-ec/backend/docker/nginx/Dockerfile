FROM nginx:stable

ENV TZ=Asia/Tokyo

ARG BACKEND_APP_DOMAIN
ENV BACKEND_APP_DOMAIN=$BACKEND_APP_DOMAIN

ARG RESOLVER
ENV RESOLVER=$RESOLVER

ARG LOG_SOURCE_NAME
ENV LOG_SOURCE_NAME=$LOG_SOURCE_NAME

COPY default.conf.template /etc/nginx/templates/

EXPOSE 80